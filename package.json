{
  "name": "hallucination-resistant-legal-research-assistant",
  "version": "2.0.0",
  "description": "No-LLM FastAPI RAG legal issue verifier with chat UI, scoring, citations, and saved history.",
  "scripts": {
    "dev": "uvicorn backend.app.main:app --reload --port 8000",
    "start": "uvicorn backend.app.main:app --host 0.0.0.0 --port 8000",
    "test": "python -m unittest discover tests",
    "vercel-build": "echo FastAPI backend and static chat UI ready"
  },
  "keywords": [
    "rag",
    "legal-tech",
    "hallucination-detection",
    "citation-verification",
    "ai-safety"
  ],
  "private": true,
  "license": "UNLICENSED",
  "dependencies": {}
}
